import styled from 'styled-components';

export const RADIOUS = 4;

export const Button = styled.button`
  padding: 0.5em 0.8em;
  background: rgba(0, 0, 0, 0.25);
  color: rgba(255, 255, 255, 0.8);
  border-right: 0;
  border-radius: ${RADIOUS}px;
`;
